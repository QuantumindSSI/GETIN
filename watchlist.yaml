# Testnet activity watchlist for the task scheduler.
# NOTE: swap and stake actions use dummy contract addresses.
# These are for development/testing only — no real tokens are traded.
- name: Monad
  network: testnet
  actions: [faucet_claim, swap, stake_apriori, nft_mint]
  frequency_days: 1
  note: "All contract actions use dummy addresses — for testing only"
- name: Somnia
  network: testnet
  actions: [faucet_claim, swap, quest_complete]
  frequency_days: 1
  note: "All contract actions use dummy addresses — for testing only"
- name: Berachain
  network: testnet
  actions: [faucet_claim, swap, lp_deposit]
  frequency_days: 2
  note: "All contract actions use dummy addresses — for testing only"
